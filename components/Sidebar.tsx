import React from 'react';

const Sidebar: React.FC = () => {
    // This component is not currently used but is given a valid structure
    // to prevent module loading errors caused by empty files.
    return null;
};

export default Sidebar;